[package]
name = "trellis"
version = "0.1.0"
edition.workspace = true

[dependencies]
engine = { path = "../engine" }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "signal", "net", "io-util", "time"] }
